setInterval(() => {
    console.log('Running!');
}, 2000);
console.log('I will run first');

/*Callback function is immediately offloaded, and keeps on invoking once all the tasks have been executed*/